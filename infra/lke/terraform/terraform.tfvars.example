# Copy to terraform.tfvars and fill in. Do not commit.

token = "your-linode-api-token"

# region        = "us-ord"
# k8s_version   = "1.34"
# label         = "ai-hackday-workshop"

# cpu_node_type  = "g6-dedicated-8"
# cpu_node_count = 5
# gpu_node_type  = "g2-gpu-rtx4000a1-s"
# gpu_node_count = 5

# Restrict ingress to a specific CIDR (e.g. classroom NAT) instead of open internet.
# allowed_cidr = "0.0.0.0/0"
